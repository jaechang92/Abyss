# `ancient_shield` — 고대 방패병

> UI 이름 **고대 방패병** · `hp x1.5 · 속도 x0.85 · 점프 1`
> 작성 2026-09-10 · 형식은 `dark_blade.md` 기준

---

## ① 정체

**들은 것** (USP-2): 넷은 **같은 사람이 형태를 바꾼 것**이다. 같은 체격 · 같은 키 · 얼굴 안 보임.

방패병은 넷 중 **가장 무거운 쪽**이다 — hp 1.5(최고) · 속도 0.85(최저) · **점프 1회**(최저).
공중에 거의 없고 **서 있는 시간이 가장 긴** 폼이다.

⚠️ **폼 이름의 「고대」는 그리지 않는다.** `ancient` 는 소설 0회 어휘이고
`00_core.txt [FORBIDDEN]` 에 올라 있어 프롬프트에 쓰면 빌드가 멈춘다.
이름은 게임이 붙인 것이고 **시각 브리프는 이름이 아니라 스탯과 실루엣에서 뽑는다.**

## ② 소설 근거

⚠️ **`00_SOURCE/quotes-figure.md` 가 아직 없다.** 아래는 **들은 것**이다.

**들은 것**: 이 세계에 「유적·신전·왕좌」는 없다(전부 0회). 방패는 **오래된 유물이 아니라
지금 쓰는 연장**으로 그린다 — stage1 앵커가 「연장이 걸린 작업장 벽」이었던 것과 같은 태도다.

## ③ 시각 브리프

### 실루엣 — **밀도는 맞다. 사람이 사라진 것이 문제다** (잰 것)

`10_BIBLE/08-silhouette §4` 실측:

```
궁수 vs 방패 0.49   ← 여섯 쌍 중 가장 잘 갈린다
방패 vs 투척 0.58   ← 여섯 쌍 중 가장 안 갈린다
채움 71.0%          ← 넷 중 압도적. hp 1.5 · 속도 0.85 와 맞는다
```

🔑 **71% 는 고칠 결함이 아니라 살릴 신호다.** 밀도가 무게를 말하고 있고
(`08-silhouette` 「밀도 = 무게」) 스탯 순서의 맨 위가 이 폼이다.

🔴 **진짜 문제는 다른 데 있다 — 덩어리가 몸을 다 덮어 사람으로 안 보인다.**
64px 실루엣에서 이 폼은 「방패를 든 사람」이 아니라 **사각 물체 하나**로 읽힌다.
①의 *「같은 사람이 형태를 바꾼 것」*이 여기서 깨진다.

| | 배정 | 근거 |
|---|---|---|
| **실루엣 도형** | **사각** | `08-silhouette §4` |
| **그 도형을 만드는 것** | 탑실드 | 〃 |
| **🔴 배치** | **방패를 몸 옆으로 뺀다.** 사각은 유지하되 **머리·어깨·다리 한쪽이 실루엣에 남아야** 한다 | 사람으로 안 보이는 것이 이 폼의 결손 |
| **채움 면적** | **70% 안팎 유지.** 넷 중 가장 높아야 한다 | 밀도 = 무게 |
| **투척사와의 거리** | 🔴 **가장 안 갈리는 쌍(0.58)이다.** 투척사가 망토를 넓히면 더 붙는다 — 둘을 **같이** 판정할 것 | 실측 |
| **점프 1회** | 공중 자세가 하나뿐이라 **선 자세의 실루엣이 이 폼의 거의 전부**다 | ① |
| 밑단 · 램프 · 윤곽선 | `dark_blade.md ③` 과 같다 | `08-silhouette §2,3` |

### 안 그리는 것

- 🔴 **강조색 없음.** 옛 배정 「수호 금색 `#FFCC52`」는 **축 색과 완전히 동일했다**(`04-palette §1`). 폐기
- `ancient` · `temple` · `throne` 등 금지 어휘 (`00_core.txt [FORBIDDEN]`)
- 광원·발광 없음 (L1) · 초록 없음 (P5)

## ④ 뽑을 것 — 상태 9종

```
Idle   Run   Jump   Fall   Dash   AttackLight   AttackHeavy   Hit   Dead
```

💡 **넷 중 시트 위험이 가장 낮다** — 방패는 폭이 넓지만 몸 안쪽에 있어 옆 프레임을 덜 침범한다.
다만 **몸 옆으로 빼는 결정(③)이 그 이점을 없앤다.** 배치를 바꾸면 간격도 다시 본다.

## ⑤ 미결

| 미결 | 어디서 정하나 |
|---|---|
| 방패를 빼면서 **채움 70% 를 유지**할 수 있는가 — 둘이 반대 방향이다 | 첫 라운드 |
| 투척사와 0.58 에서 얼마나 내려가는가 | `void_thrower.md` 와 짝으로 판정 |
| 사람 윤곽 중 **무엇을 남기나** (머리 / 어깨 / 다리) | 첫 라운드 실루엣 판정 |
| 소설 근거 (`quotes-figure.md`) | `00_SOURCE` 착수 시 |

---

<!-- BUILD-INPUT -->

# 🔴 [BLOCK] 과 '#' 주석만. 한글은 '#' 뒤에만.
# 🔴 `ancient` 는 [FORBIDDEN] 이다 — 폼 이름을 그대로 문구에 쓰지 않는다.
# ⚠️ 조립기는 아직 이 구역을 안 읽는다 (③ 단계에서 붙인다).

[IDENTITY]
the same person each time, same build and height, face never visible

[WEAPON]
# 🔑 사각은 유지하되 사람이 남아야 한다 — 머리와 한쪽 다리가 방패 밖으로 나온다.
a tall square tower shield carried at one side, head and one leg still clear of it

[WEAR]
# 가장 무거운 폼 — 넷 중 가장 두껍고 채움이 높다.
heavy plated gear, thick and solid, cut even at the hem

[SILHOUETTE]
one solid square beside a standing figure, the figure still readable

[STATES]
Idle, Run, Jump, Fall, Dash, AttackLight, AttackHeavy, Hit, Dead
