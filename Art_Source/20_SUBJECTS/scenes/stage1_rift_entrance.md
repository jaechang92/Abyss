# `stage1_rift_entrance`

> UI 이름 **균열의 입구** · 보스 `AbyssKeeper` · 5~7방 + 보스 (`08-content-roadmap §6-1`)
> **첫 씬이고 앵커를 여기서 먼저 뽑는다.** 작성 2026-09-06

---

## ① 정체

> **아직 아무것도 안 없어졌다. 위와 같은데 잘못 놓여 있다.**

없는 것: **없음.** 8개 씬 중 유일하다 — 그래서 다음 씬부터의 결여가 읽힌다.

## ② 소설 근거

**잰 것** — 하란이 처음 내려간 층은 **자기 세계의 밑바닥**이다. 다른 세계가 아니라 **익숙한 것이 잘못 놓인 곳**이다
(집필계획 §3 1권 8장: *"1층: 아직 자기 세계다. 익숙한 것들이 잘못 놓여 있다"*).

그 앞의 경계는 이렇게 생겼다:

> 등불을 내밀어도 빛이 아래로 가지 않았다. **빛이 선을 넘는 순간 없어졌다.** — `1권/04-그날 밤.md:146`
> 빛이 닿지 않는 것이 아니라, **빛이 그 선을 넘으면 없어졌다.** — `1권/08-목록.md:199`

## ③ 시각 브리프

| | 배정 | 근거 |
|---|---|---|
| **빛의 거동** | **선을 넘으면 없어진다** — 그라데이션이 아니라 뚜렷한 선에서 끊긴다 | `1권/04:146`·`08:199` |
| **램프 위치** | 중간 | `01-world-identity §5` |
| **잔여 색상** | 차가운 회청 | P2 |
| **층 강조** | **없음** | 첫 화면 |
| **재질** | 돌 · **익숙한 살림이 잘못 놓인 것** | W2 |
| **레이어** | sky · far · mid · near (기본형) | `06 §2` |

### 🔴 gen3에서 바꾸는 것 — 셋 다 새 규약 위반이다

| gen3 | 위반 | 새 브리프 |
|---|---|---|
| `collapsed temple ruins at the mouth of a rift` | **W2** (폐허 아님) · **W4** (`temple`·`rift` 0회 어휘) | 무너진 유적이 아니라 **어제까지 멀쩡했던 곳.** 살림이 그대로인데 자리가 어긋나 있다 |
| `one pale shaft of light from far overhead` | 🔴 **L1** (광원 금지) · **L2** (방향 그림자) | 광원 없음. **읽히던 표면이 한 선에서 끝난다** |

> 🔴 **2026-09-07 첫 앵커 호출에서 위 줄을 다시 어겼다.** gen3 를 고쳐 놓고도 4장 전부 광원이 나왔다.
> 새 브리프가 *"빛은 선에서 끊길 뿐 어디서도 안 온다"*였고 그것을 그대로 영어 문구로 옮겼기 때문이다 —
> **「빛이 끊긴다」는 여전히 빛을 그리라는 말이다.** 끊기는 것이 보이려면 웅덩이가 있어야 한다.
> 그래서 브리프 자체를 빛이 아니라 **보이는 것**으로 다시 썼다(위 표 오른쪽 칸). 자세한 것은 `[LIGHT]` 주석.
| `dark moss` (팔레트 `3A4A38`·`55663F`) | 🔴 **P5** (어비스에 초록 없음) | 제거. 초록은 시엔과 지상 전용이다 |

> 💡 **「유적」을 버리는 것이 이 씬에서 가장 큰 변화다.**
> 유적은 오래된 것이고 이 씬은 **어제까지 사람이 쓰던 곳**이다.
> 익숙한 것이 조금씩 어긋나 있는 쪽이 무너진 신전보다 무섭고, **소설 1층이 정확히 그렇다.**

### 살아남는 것 — gen3의 `[STORY]` 둘

| gen3 소재 | 판정 |
|---|---|
| `a stairway that ends in open air` | ✅ **유지.** 계단이 하강의 유일한 형식이고(C2), *"위를 향하다 만 것"*은 소설의 삼천 단과 이어진다 |
| `a wall of tally marks left by someone counting days` | ✅ **유지 · 강화.** 🔑 소설 전체가 **세는 이야기**다. `3권/21`의 **벽의 글자가 줄어드는** 것으로 확장할 수 있다 |
| `a shattered seal slab` | ❌ **버린다.** 「봉인」은 소설 어휘가 아니다. 대신 **경계 그 자체** — 빛이 끊기는 선 |

## ④ 팔레트 1차안

**무채 회청 램프 9단 + 가장 밝은 1 = 10색. 층 강조 없음.**

```
0B0E11   141A20   212A33   2F3B45   435059
5E6B72   7C8A8C   99A6AC   BAC4C9   D2D9DB
```

| 검사 | 결과 |
|---|---|
| **P4** 순검정·순백 아님 | ✅ `0B0E11` / `D2D9DB` |
| **P1** 잔여 채도 있음 | ✅ 전 단계가 청색 쪽으로 치우쳐 있다 |
| **P5** 초록 없음 | ✅ gen3의 이끼 2색 제거 |
| **S3** 축 6색과 명도·채도에서 갈림 | ✅ 채도가 전부 매우 낮다. 축 6색은 전부 고채도 |
| **P2** 씬 잔여가 회청으로 일관 | ⚠️ gen3의 `A6B0AC`(초록 쪽)·`C9CBBF`(누런 쪽)를 청색 쪽으로 되돌렸다 — **stage3의 누렁과 겹치는 것을 막기 위해서** |

> ⚠️ **이 열 개는 손으로 고른 1차안이다.** 앵커를 뽑은 뒤 `measure_consistency.py`로 재서 조정한다.
> 🔴 **팔레트가 앵커보다 먼저여야 한다** — 팔레트가 **앵커를 고르는 판정 기준**이기 때문이다.
> 기준 없이 뽑은 앵커는 무엇과 비교해 고를지가 없고, 그 한 장이 이 씬 전부의 화풍을 정한다.
>
> 💡 **층 강조가 없는 것이 이 씬의 설계다.** stage1은 나머지 씬이 벗어나는 **기준선**이고,
> 축 6색(신호)과 가장 안 부딪히는 자리다. 여기서 플레이어가 *"무엇이 내 캐릭터인가"*를 배운다.

## ⑤ 뽑을 것 — 14 요청

```
배경  bg_sky  bg_far  bg_mid  bg_near  bg_boss_arena
타일  tileset_ground  tileset_wall  tile_platform
프롭  prop_decor  prop_hazard  prop_landmark
      prop_interactive ×3
```

**앵커 파트**: `bg_mid` — 여러 장 뽑아 **사람이 한 장 고른다.** `anchors/stage1_rift_entrance.png`

> 🔴 **이 한 장이 스테이지 1의 화풍 전부를 결정하고, 나머지 스테이지 앵커도 이것을 참조한다.**
> 여기서 타협하면 130장이 그 타협을 물려받는다. 앵커 다시 뽑는 값 < 씬 하나 다시 뽑는 값.

### ✅ 확정 (2026-09-08) — 5라운드 · 20장 · 125 generations

**채택본**: R5 cand2 — 연장이 걸린 작업장 벽. `anchors/stage1_rift_entrance.png`
(**159×95 · 크롭 + 팔레트 집행 후.** 저장 단계는 아래 참조)

```
팔레트 밖 0 px · 흰 0 px · 최암 #0B0E11 · 쓰인 단 7/10 (0B0E11 ~ 7C8A8C)
평균 ΔE 5.2 · 상위5% 5.6 · 따뜻 0.8% · 초록 0.6% · 좌우 끝단차 26.6 · 실루엣 sd 2.2
```

- 규약 13항목 **전원 통과.** 라운드 5 는 네 장이 모두 통과한 첫 라운드였고,
  가른 것은 규약이 아니라 **중경 층으로서의 쓸모**였다(실루엣 sd → `10_BIBLE/03 §6`).
- 내용이 씬 ① 과 맞는다 — 연장이 벽에 걸린 채 작업대와 의자가 그대로다.
  **「없는 것: 없음」**을 가장 직접적으로 말하는 화면이라 다음 씬부터의 결여가 여기에 대비되어 읽힌다.

> 🔑 **쓰인 단이 아래 7개뿐이고 위 3단(`99A6AC`·`BAC4C9`·`D2D9DB`)이 비어 있다.**
> 이것은 결손이 아니라 **자리를 비워 둔 것**으로 읽는다 — 배경이 아래 7단을 쓰고
> 캐릭터가 위 3단을 쓰면, `10_BIBLE/03 §6` 의 미결 「명도로 못 가르면 무엇이 가르나」에
> 답이 하나 생긴다. **파생 12장을 뽑을 때 이 분할이 유지되는지 볼 것.**

**저장 단계 — 크롭 + 팔레트 집행 후로 정했다** (문서에 규정이 없던 자리):
파생 12장도 전부 집행을 거치므로, 앵커가 집행 전이면 파생이 `style_copy=color_palette` 로
**최종 색이 아닌 색을 베낀다.** L3(순검정)도 같이 해소된다.
크롭은 필수다 — 산출물에 흰 거터가 딸려 온다(`40_TOOLS/pixellab/profile.md §6-B`).

**5라운드에서 얻은 문구 작성 법칙은 `30_BLOCKS/WRITING.md` 로 뽑았다.**
남은 씬 7개는 그 체크리스트를 먼저 통과시킨 뒤 뽑는다.

**앵커 판정**: `10_BIBLE/03-light.md §5` 체크리스트를 그대로 쓴다. 이 씬에서 특히 볼 것 —

```
☐ 위에서 내려오는 빛줄기가 있는가        → 있으면 탈락 (gen3가 이걸 명시적으로 요청했었다)
☐ 이끼·풀·초록이 있는가                  → 있으면 탈락 (P5)
☐ 무너진 신전·기둥·유적으로 보이는가      → 그러면 탈락 (W2)
☐ 양옆이 벽·아치로 막힌 「방 한 칸」인가   → 그러면 탈락 (가로로 안 이어진다)
☐ 화면 중앙이 캐릭터가 설 만큼 조용한가
```

> 🔴 **네 번째 줄은 2026-09-07 후보 4장이 전부 걸려서 생겼다.** 넷 다 아치로 닫힌 실내 한 칸이었다.
> `bg_mid` 는 가로로 스크롤되는 패럴랙스 층이라 양옆이 막히면 이어 붙지 않는다.
> 판정 목록(`30_BLOCKS/parts/bg_mid.txt` 머리말)에는 「가로 양끝이 이어지는가」가 원래 있었는데
> **프롬프트에는 없었다.** 광원과 달리 이건 긍정으로 부탁할 수 있는 항목이라 `[BODY]` 에 넣었다 —
> 💡 **판정으로만 막을 것과 말로도 부탁할 것을 가르는 기준**: 그리지 *말라*는 것은 판정으로,
> 그려 *달라*는 것은 문구로. 부정문은 모델에게 약하고, 긍정문은 판정으로 옮길 이유가 없다.

## ⑥ 미결

| 미결 | |
|---|---|
| 「잘못 놓인 살림」을 32px 타일과 48px 프롭으로 어떻게 보여 주나 | `05-material` + 프롭 카탈로그 |
| ~~빛이 끊기는 선을 배경 레이어 어디에 두나~~ → **중경(`bg_mid`)에만.** 2026-09-08 확정 | `bg_far` 에서 `[USE-SCENE] LIGHT` 제거. 선이 여러 층에 있으면 패럴랙스에서 서로 다른 속도로 미끄러진다 — 하나여야 선이다 |
| `bg_boss_arena`(AbyssKeeper)를 이 팔레트로 가나 | 보스 씬은 강조가 필요할 수 있다 |

---

<!-- BUILD-INPUT -->

# ══════════════════════════════════════════════════════════════
# 아래가 50_BUILD/build.py 가 읽는 구역이다. 위쪽 산문은 통째로 무시된다.
# 🔴 여기에는 [BLOCK] 과 '#' 주석만 둔다. 표(|)가 섞이면 빌드가 멈춘다.
# 🔴 한글은 반드시 '#' 뒤에만. 본문에 섞이면 빌드가 멈춘다.
# ══════════════════════════════════════════════════════════════

[ANCHOR-PART] bg_mid

[SETTING]
# 「폐허·고대 유적」을 버린 자리. 유적은 오래된 것이고 이 씬은 어제까지 쓰던 곳이다.
the underside of an ordinary town, everyday things set down wrong

[MATERIAL]
# 재질만 남긴다. 정체는 SETTING 이 말하고 살림은 MOTIF(left) 가 말한다 —
# 셋이 같은 말을 하면 문구 예산만 먹고 서로 흐려진다.
#
# 🔴 2026-09-08 (3차) — `plain grey` → `cold grey-blue`. **색이 처음으로 문구에 실린다.**
#
#    4라운드 16장의 계측이 이 자리를 가리켰다: 따뜻한 픽셀 1.4~61.6%, 초록기 0~26.5%.
#    이만한 산포는 모델이 흔들린 게 아니라 **색에 대해 아무 말도 못 들었다**는 뜻이다.
#    실제로 못 듣는다 — 세 경로가 전부 앵커를 비켜 간다:
#      · 팔레트는 후처리(enforce_palette.py)가 집행한다 → 생성 요청에 안 실린다
#      · style_copy = color_palette 는 파생 전용이다 → 앵커에는 style_image 가 없다
#      · 남은 색 단어는 여기 `grey` 하나뿐인데 **회색은 어느 색조로도 기운다**
#
#    🔑 그래서 규칙을 어기지 않고도 갈색 회색(R4 cand0, 따뜻 39.6%)과
#       초록 회색(R4 cand3, 초록기 26.5% → P5 위반)이 나왔다. 낱말은 다 지켰다.
#    📌 **집행할 수 있다는 것이 말하지 않아도 된다는 뜻은 아니다.** 후처리는 색을 옮길 뿐
#       구도를 못 바꾼다 — 초록 이끼로 그려진 벽은 회청으로 칠해도 이끼 벽이다.
#
#    씬 ④ 가 팔레트를 「차가운 회청」으로 못박아 두고도 그 말이 프롬프트에 한 번도
#    안 실렸다. 3단어 그대로라 예산 변화도 없다.
#    💡 곁다리로 `plain` 중복이 3회 → 2회로 준다(TONE 과 [LIGHT] 에 남는다).
cold grey-blue stone

[GROUND-MATERIAL]
# 🔴 물건 없이 면만. 32px 타일에 물건이 들어가면 바닥마다 반복된다.
flat worn stone slabs, one even surface

[WALL-MATERIAL]
# gen3 는 벽이 배경 재질을 그대로 썼다. 이제 따로 적는다.
#
# 🔴 2026-09-09 — `deep dark` 를 넣었다. **잰 것:**
#    지면 타일셋에 `the same dark stone throughout` 을 넣어 어둡게 만들었더니
#    벽이 상대적으로 **밝아졌다**(평균 단 지면 2.87 vs 벽 3.66). 규약은 그 반대다 —
#    벽이 화면에서 가장 넓으므로 가장 어둡고 조용해야 한다(tileset_wall.txt 머리말).
#
# 🔑 값(명도)은 **재질 어휘가 말할 것**이지 타일셋 블록이 말할 것이 아니다.
#    타일셋 블록에 `the darkest surface in the room` 을 적어 봤지만 안 먹었다 —
#    「방」이 무엇인지 모델은 모른다. **재질 이름에 붙이면 어디에 쓰이든 따라간다.**
deep dark stone wall face, quiet and even

[SKY]
# 🔴 구름을 그리지 않는다(L7). 하늘은 텍스처가 아니라 면이다.
a low flat grey ceiling of nothing

[LIGHT]
# 🔴 이 블록에 `light` 를 쓰지 않는다. 이제 [FORBIDDEN] 어휘라 쓰면 빌드가 멈춘다.
#
#    2026-09-07 앵커 후보 4장이 전부 L1(광원)로 탈락했고, 원인이 정확히 이 자리였다.
#    앞 문구는 `light stops at one hard straight edge` 였고 금지는 이 주석에만 있었다.
#    조립기는 '#' 을 걷어낸다(옳은 동작 — 한글이 섞이면 모델이 지시로 읽는다).
#    모델에게 남은 것은 "빛이 있고 그것이 단단한 직선에서 멈춘다"뿐이다.
#    멈추는 것이 보이려면 웅덩이가 있어야 하고, 웅덩이가 있으려면 광원이 있어야 한다.
#    🔑 **문구가 광원을 말하지 않으면서 요구했다.** 금지어 검사는 낱말을 보므로 안 걸렸다.
#
# 🔑 그래서 빛이 아니라 **보이는 것**을 적는다 — 03-light L4 의 직역이다
#    (어둠은 명도가 아니라 정보의 부재다. 어두워서 안 보이는 게 아니라 볼 것이 없다).
#    이 씬의 거동 「선을 넘으면 없어진다」는 밝기 이야기가 아니라
#    **읽히던 표면이 한 선에서 끝난다**는 이야기다. 그러면 광원이 필요 없다.
#
# ⚠️ 8단어가 상한이다. 이 블록은 bg_mid·bg_boss_arena 가 같이 싣고, 예산이 가장
#    빡빡한 쪽이 한계를 정한다. 늘리려면 [WORD-BUDGET] 이 아니라 문구를 줄일 것.
#
# 🔴 2026-09-08 (2차) — `blank` 를 뺐다. 이 낱말이 **산출물의 이름 그 자체**였다.
#    3라운드에서 4장 전부에 평평한 밝은 블록(#E4E4E4)이 뚫렸다. 용의자가 둘이었고
#    (`blank past it` / bg_mid 의 `open at both sides`) 둘 다 2라운드에는 있었지만
#    그때는 멀쩡했다 — 시점이 정측면으로 바뀐 뒤에야 터졌다.
#
#    🔑 둘은 사실 **같은 교훈의 두 사례**다: 비어 있음을 이름으로 부르지 말 것.
#       모델은 `blank` 를 「정보 없음」이 아니라 「칠하지 않은 면」으로 그린다.
#       10_BIBLE/03 L4 가 요구하는 것은 밝은 공백이 아니라 **볼 것이 없는 표면**이다.
#
#    → `plain past it`. 무늬가 없다는 뜻이라 면은 여전히 칠해진다.
#      [TONE] 의 `plain` 과 같은 낱말이라 어휘도 안 갈린다. 8단어로 길이도 같다.
detail stops at one line, plain past it

[MOTIF] left

[STORY]
# 인터랙티브 프롭. 줄마다 한 장씩 뽑는다([SPLIT-SCENE]).
# 셋 다 소설에서 왔다 — 삼천 단 계단 / 벽의 글자(3권 21장) / 빛이 끊기는 선(1권 4장).
#
# 🔴 세 번째 줄이 [LIGHT] 와 같은 병을 앓고 있었다 — `a doorway where the light ends`.
#    여기는 더 나빴다. 프롭에는 params.txt [NOTICEABLE] 이 함께 실려서 조립본이
#    "빛이 끝난다 + 주변보다 덜 어둡다 + not lit" 한 문장이 됐다. 서로 모순이라
#    모델이 고를 수 있는 답은 「빛나는 문」 하나뿐이다.
#    [LIGHT] 와 같은 어휘로 맞춘다 — 두 자리가 같은 것을 말하므로 갈리면 안 된다.
a stairway that stops partway up
a wall covered in tally marks left by someone counting days
a doorway where detail stops at one line, plain past it

[TILE-OUTER]
dark empty air

[PARTS]
bg_sky, bg_far, bg_mid, bg_near, bg_boss_arena,
tileset_ground, tileset_wall, tile_platform,
prop_decor, prop_hazard, prop_interactive, prop_landmark
