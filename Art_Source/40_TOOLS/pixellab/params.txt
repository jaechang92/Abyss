# ══════════════════════════════════════════════════════════════
# PixelLab Pro (v2) 생성 파라미터 + 앵커 규약
#
# 🔥 이 파일은 **버리는 층**이다. 도구가 바뀌면 40_TOOLS/ 만 새로 쓰고
#    00_SOURCE / 10_BIBLE / 20_SUBJECTS / 30_BLOCKS 는 그대로 간다.
#    넉 달에 도구가 세 번 바뀌었고 그때마다 정체성까지 같이 죽었다. 그래서 나눴다.
#
# 🔑 2026-09-06 Pro 전환에서 이 구조가 처음으로 값을 했다 —
#    엔드포인트를 통째로 갈았는데 위 네 층은 한 글자도 안 바뀌었다.
#
# 🔴 v2 에서 사라진 칸 셋. 전부 다른 데로 옮겼다:
#      color_image           → 후처리 Tools/ArtPipeline/enforce_palette.py 가 집행
#      negative_description  → 판정 목록 (조립본 .md 체크리스트)
#      outline/shading/detail/view → 아래 [STYLE-WORDS] 가 문구로 갖고 있다
#    자세한 것은 profile.md §0.
#
# 🆕 2026-09-07 PixelLab MCP 연결. **도구를 바꾼 것이 아니라 같은 도구에 손이 닿은 것**이다.
#    그래서 이 파일은 새로 쓰지 않고 **틀린 곳만 고쳤다** — 그 구분이 중요하다.
#    profile.md §6 에 "첫 실호출 때 채운다"고 적어 둔 미확인 6개가 전부 확정됐고,
#    그중 둘은 적어 둔 내용이 **사실과 반대**였다([PARAMS-TILESET]·[PARAMS-PROP]).
#    ⚠️ 값을 지어내 채우지 않은 덕에 무엇이 틀렸는지 알 수 있었다. 그 습관을 유지할 것.
# ══════════════════════════════════════════════════════════════

[ANCHOR]
# 🔴 이 파이프라인의 중심 규약. 순서를 지키지 않으면 전부 다시 뽑는다.
#
#   1. 씬마다 **앵커 한 장**을 style_image 없이 뽑는다 (bg_mid).
#      여러 장 뽑아 사람이 고른다 — 이 한 장이 그 씬의 화풍 전부를 결정한다.
#   2. 고른 앵커를 Art_Source/anchors/<scene>.png 로 저장한다.
#   3. 그 씬의 **나머지 전부**는 style_image = 그 앵커.
#
# 💡 legacy 는 앵커/파생이 서로 다른 모델이었다(pixflux → bitforge).
#    v2 는 **같은 엔드포인트에 style_image 유무로만 갈린다.** 규약은 그대로고 수단만 단순해졌다.
#
# 🆕 2026-09-07 MCP 연결로 확정 — style_strength 대응 칸은 **없다.**
#    대신 `style_copy` 가 있다: 앵커에서 무엇을 베낄지 **항목으로 고른다**
#    (color_palette / outline / detail / shading 중 고르며, 안 주면 넷 다).
#    🔑 강도 조절이 아니라 **선택적 복사**다. 이게 오히려 우리 쪽에 맞는다 — 아래 [STYLE-COPY].
#
# 🔴 다른 씬의 앵커를 style_image 로 쓰지 않는다.
#    예외는 hidden 하나 — 거울상 영역이라 의도적으로 stage1 앵커를 쓴다([STYLE-FROM]).
#
# 🔴 한 호출이 후보를 **여러 장** 준다 (캔버스가 작을수록 많다):
#      ≤42px 64장 · ≤85px 16장 · ≤170px 4장 · 그 위 1장
#    160x96 은 최장변 160 이라 **한 호출에 4장**이다.
#    💡 그래서 "앵커를 여러 장 뽑아 고른다"가 여러 번 호출이 아니라 **한 번 호출**이다.
#    ⚠️ 비용은 후보 수가 아니라 **호출당**이다 — 4장을 받든 1장을 받든 같은 값을 낸다.
tool = create_image_pro
anchor_needs_style_image = false

[STYLE-COPY]
# 🆕 2026-09-07 신설. `style_copy` 에 실을 항목을 파트 종류별로 정한다.
#
# 🔴 넷 다 베끼면(기본값) 배경 앵커의 `lineless` 가 발판·프롭까지 따라간다.
#    그런데 [PARAMS-PLATFORM]·[PARAMS-PROP] 은 `outline = selective` 를 명시한다 —
#    **앵커가 우리 지정을 덮어쓴다.** 배경만 lineless 인 것은 연출이 아니라
#    가독성 장치다(무엇이 조작 대상인지 가르는 선). 기본값을 쓰면 그 장치가 죽는다.
#
# 🔑 그래서 파생 종류마다 베낄 항목이 다르다:
#      배경 레이어  넷 다 — 같은 판의 다른 층이라 선·밀도까지 같아야 한다
#      발판·프롭    색과 명암만 — 윤곽선·밀도는 제 지정을 지킨다
#
# ⚠️ color_palette 를 여기서 베끼는 것과 후처리 집행은 **다른 일**이다.
#    여기는 적중률을 올리는 보조 수단이고, 집행은 enforce_palette.py 가 한다.
bg = color_palette, outline, detail, shading
sprite = color_palette, shading

[PALETTE]
# 🔴 이 블록이 게임 가독성을 지킨다. 빼면 캐릭터가 배경에 묻힌다.
#
# ⚠️ **2026-09-06 — 집행 지점이 바뀌었다.**
#    예전에는 여기에 color_image 를 적었고 그것이 강제였다. v2 Pro 에는 그 칸이 없다.
#    이제 강제는 **생성 뒤**에 일어난다:
#
#        python Tools/ArtPipeline/enforce_palette.py raw/*.png --scene <scene>
#
#    🔑 이것이 오히려 설계를 고쳤다 — 바이블의 핵심 장치가 *버리기로 한 층의 기능*에
#       걸려 있었다. 후처리는 도구가 네 번째로 바뀌어도 안 죽는다. (10_BIBLE/04 머리말)
#
# ⚠️ 2026-09-06 에 **피해야 할 대상도 바뀌었다.**
#    예전에는 폼 액센트 4색이었다. 이제는 **시너지 축 6색**이다 —
#    색을 폼에서 떼어 축에 줬기 때문이다(10_BIBLE/04 §1).
#
#    축 6색 (SynergyAxis.cs 가 SoT — 아트가 이 값을 정하지 않는다):
#       fire       #FF752E    blood_pact #E6384D
#       abyss      #A36BF5    frost      #73CCFF
#       guard      #FFCC52    soul       #8CF2B3
#
#    S1  신호 색을 월드에 칠하지 않는다.
#    S3  씬 팔레트는 이 6색과 **명도 또는 채도에서** 갈려야 한다.
#        색상만 피하는 것으로는 부족하다.
#
# ⚠️ 팔레트 파일에 무엇을 넣느냐가 이 파이프라인에서 가장 중요한 손작업이다.
#    색을 고르는 순간 판정이 끝난다.
# 🔴 그래서 **팔레트가 앵커보다 먼저다** — 이유는 바뀌었지만(강제 → 판정 기준) 결론은 같다.
#    앵커를 무엇과 비교해 고를지가 없으면 고를 수 없다.
enforce_after = true
palette_file = palettes/<scene>.png

[STYLE-WORDS]
# 🆕 v2 에는 outline/shading/detail/view 칸이 없다. 그 지정을 **문구로 내린다.**
#
# 🔑 **거의 항등 사상이다. 그게 맞다.**
#    legacy 의 enum 값(`lineless` `flat shading` `low detail` `side view`)은
#    애초에 영어 프롬프트 어휘 그대로였다. 칸에서 문구로 자리만 옮기면 된다.
#
# ⚠️ 처음에는 설명형으로 길게 썼다가 되돌렸다 —
#    `lineless, edges read by value not by line` 식으로 쓰면 네 필드가 20 단어가 되어
#    **[WORD-BUDGET] 40 을 스타일 지정만으로 절반 먹는다.** 파트 문구가 그만큼 밀리고,
#    PixelLab 은 문구가 길면 서로 희석되는 모델이다. 정체성을 말할 자리를 뺏으면 안 된다.
#
# 💡 그래도 이 표가 있어야 하는 이유 셋:
#      ① 이 값들이 이제 **필드가 아니라 문구**라는 사실이 여기 적혀 있다
#      ② 모델이 못 알아들으면 **고칠 곳이 여기 한 곳**이다
#      ③ 오타를 build.py 가 잡는다 — 없는 키는 경고가 뜬다.
#         (조용히 빠지면 "안 실었다"와 "모델이 무시했다"를 구분할 수 없다)
# 🔴 값은 **짧은 토큰 하나로 통일**한다 (`medium`, `low`, `high` — `medium detail` 아님).
#    첫 빌드에서 파트 파일 5줄이 전체 enum 형(`flat shading`·`highly detailed`)을 쓰고 있어
#    어휘가 갈려 있었고, build.py 경고가 그것을 잡았다. 둘 다 받게 하면 파편화가 남는다.
#    ⚠️ 새 값을 쓸 때는 반드시 여기에도 줄을 추가할 것. 없으면 경고가 뜬다(조용히 안 빠진다).
outline_lineless = lineless
outline_selective = selective outline
shading_flat = flat shading
shading_basic = basic shading
shading_medium = medium shading
detail_low = low detail
detail_medium = medium detail
detail_high = highly detailed
view_side = side view

[PARAMS-BG]
# 패럴랙스 배경 레이어. 방은 가로로 스크롤되므로 가로가 길다.
#
# 🔴 크기는 **한계가 아니라 판단이다.** 160x96 은 원래 무료 티어 면적 제한(200x200)
#    때문에 고른 값인데, Pro 에는 그 제한이 없다(최대 512x512 / 688x384).
#    그래도 안 올린다 — 688x384 로 뽑으면 픽셀 아트가 아니라
#    "픽셀처럼 생긴 일러스트"가 된다. 픽셀 밀도는 이제 우리가 정한다. (profile.md §1)
image_size = 160x96
outline = lineless
shading = basic
detail = low
view = side
# 🔑 배경만 lineless 다. 윤곽선이 있으면 배경 물체가 캐릭터만큼 또렷해져
#    무엇이 조작 대상인지 화면에서 안 갈린다. 발판·프롭은 selective 를 쓴다.
#    이 대비가 10_BIBLE/03 L3(좁은 명도 대비)의 해답 후보다 —
#    명도로 못 가르면 윤곽선 유무로 가른다.

[PARAMS-BG-SKY]
# 최원경만 예외적으로 더 흐리다 — 패럴랙스 계수가 가장 낮아 크게 안 움직인다.
# 🔴 구름을 그리지 않는다(10_BIBLE/03 L7). 하늘은 텍스처가 아니라 면이다.
image_size = 160x96
outline = lineless
shading = flat
detail = low
view = side

[PARAMS-TILESET]
# 🔴 2026-09-07 — 이 블록은 **거의 전부 틀렸었다.** MCP 로 실제 인자를 보고 고쳤다.
#    적어 둔 네 줄 중 세 줄이 사실과 달랐다. 아래가 정정 내역이다.
#
#    ❌ "API 가 아니라 웹 도구다. Pro 전환의 영향을 안 받는다"
#    ✅ **API 다** — `create_sidescroller_tileset`. 손으로 옮겨 담을 일이 없다.
#
#    ❌ "입력은 셋: inner / transition / outer"
#    ✅ **둘이다** — lower(밟는 땅의 속) + transition(윗면).
#       🔑 outer 칸이 없는 이유는 빠뜨려서가 아니라 **배경이 투명하게 나오기 때문**이다.
#          횡스크롤 타일셋은 땅이 아닌 곳을 그리지 않는다. 그 자리는 알파다.
#       ⚠️ 그래서 씬의 [TILE-OUTER](stage5 = 물)는 **타일셋으로 못 넣는다.**
#          땅 밖에 무엇이 있는지는 배경 레이어가 말한다. build.py 가 메타로 강등한다.
#
#    ❌ "transition_size 는 경계 높이 — 크면 벽이 선다"
#    ✅ **윗면 층의 두께 비율(0.0~0.5 실수)** 이다. 0 = 윗면 없음 · 0.25 = 얇게 ·
#       0.5 = 두껍게. 예전 값 `1` 은 아예 범위 밖이었다.
#       💡 [TRANSITION] 이 "평평한 한 줄"을 요구하므로 얇은 쪽이 맞다 → 0.25.
#
#    ❌ "Target Palette 칸이 있다"
#    ✅ **없다.** 팔레트는 후처리(enforce_palette.py)만이 집행한다 — 보조 수단도 없다.
#
# 🔑 반대로 **이쪽에는 남아 있는 칸**이 있다. 이미지 생성에는 없어서 문구로 내린
#    outline / shading / detail 이 타일셋에는 **필드로 있다.**
#    그래서 타일셋 파트만 [STYLE-WORDS] 를 안 거친다 (build.py 가 갈라 낸다).
#
# 🔑 base_tile_id — 문서에 없던 장치다. 먼저 만든 타일셋의 기준 타일 id 를 넘기면
#    다음 타일셋이 **그것에 이어 붙게** 나온다. 지면과 벽이 따로 놀지 않게 하는
#    유일한 수단이고, 이미지 쪽 앵커 규약에 정확히 대응한다.
#    → 순서: ground 를 먼저 만든다 → get_sidescroller_tileset 으로 id 를 받는다
#            → wall 을 그 id 로 만든다. 순서를 바꾸면 벽이 기준이 된다.
#
# ⚠️ 비용은 **호출당 2~3 generations** 다 (1 이 아니다). 소요 ~100초.
tool = create_sidescroller_tileset
tile_size = 32
transition_size = 0.25
outline = selective
shading = basic
detail = medium
# 구조 엄격도. border_jitter 는 없는 칸이었고, 실제 대응물은 이 둘이다.
# 🔴 윗면이 울퉁불퉁하면(=콜라이더와 어긋나면) freedom 을 낮춘다. 기본 500.
tileset_adherence = 100
tileset_adherence_freedom = 500
tile_strength = 1

[PARAMS-PLATFORM]
# 떠 있는 발판은 타일셋이 아니라 낱개다(사방이 뚫려 있어 Wang 이 안 맞는다).
image_size = 48x16
no_background = true
outline = selective
shading = basic
detail = medium
view = side

[PARAMS-PROP]
# 장식·트랩·인터랙티브. 알파 필수 — 배경 위에 얹는 것이다.
#
# 🆕 2026-09-07 확정 — `no_background` **있다.** chroma_cutout.py 는 안 쓴다.
#
# 🔴 그런데 함정이 반대 방향에 있다. Pro 의 기본값이 **true** 다
#    ("pro is normally used for assets"). 즉 **배경 레이어에서 이 칸을 빠뜨리면
#    하늘·먼 배경이 투명하게 나온다.** 프롭이 아니라 배경이 위험한 칸이다.
#    → build.py 는 이 값을 **항상 실어 보낸다**(기본 false). 빠지는 경우가 없어야 한다.
image_size = 48x48
no_background = true
outline = selective
shading = medium
detail = medium
view = side

[SILENT]
# 장식·트랩용. 배경에 녹아들어야 한다.
muted and dusty, blends into the wall

[NOTICEABLE]
# 인터랙티브 전용. 어두운 방에서 플레이어가 찾아낼 수 있어야 한다.
#
# 🔴 2026-09-06 변경 — gen3 는 `faint pale glow` 였다.
#    10_BIBLE/03 L1(광원 금지)에 걸린다. 빛나는 프롭은 광원이다.
#    발광을 빼고 **주변보다 덜 어둡게**로 바꿨다. `glow` 는 이제 빌드 실패 어휘다.
#
# 🔴 2026-09-08 — 끝의 `not lit` 을 뗐다. 안전장치로 붙여 둔 것인데 반대로 작동한다.
#    00_core.txt [TONE] 이 `not ruined` → `still standing` 으로 겪은 것과 같은 자리다:
#    **모델은 "lit 를 빼라"가 아니라 "lit"를 읽는다.** 앞의 「주변보다 덜 어둡다」가
#    이미 같은 말을 긍정으로 다 하고 있으므로 두 단어는 위험만 남긴다.
#    같은 날 씬 [LIGHT] 가 `light stops at…` 로 광원을 불러온 것과 한 계열이다.
less dark than what is around it, easy to spot

[SETTINGS]
# 🔴 한 씬의 레이어들은 반드시 한 세션 안에서 끝낸다.
#    패럴랙스는 여러 장이 겹쳐 보이는 구조라 색온도 차이가 그대로 층으로 드러난다.
#    날짜를 넘겨 이어 뽑으면 같은 설정이어도 갈린다 — gen2·gen3 가 둘 다 적어 둔 것이다.
#
# 🆕 2026-09-07 확정 — `seed` **있다.** 이미지·타일셋 양쪽 다 있다.
#    "한 세션 규약이 유일한 방어선"이던 상태는 풀렸다. 다만 규약을 약하게 두지 않는다 —
#    seed 는 같은 인자에 같은 그림을 주는 장치이고, **인자가 바뀌면 아무것도 보장하지 않는다.**
#    한 씬의 레이어들은 문구가 서로 다르므로, seed 고정은 세션 규약의 대체가 아니라 보강이다.
#
#   seed          한 씬 안에서 고정 (프롭만 예외 — 서로 닮으면 안 된다)
#   style_image   그 씬의 앵커. 절대 다른 씬 것을 쓰지 않는다
#   팔레트         생성이 아니라 **생성 뒤**에 집행한다 (enforce_palette.py)
