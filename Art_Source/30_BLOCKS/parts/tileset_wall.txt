# ══════════════════════════════════════════════════════════════
# 벽면 타일셋
#
# 지면과 별개로 뽑는다. 한 타일셋으로 둘 다 하면 한쪽이 반드시 어색해진다.
#
# 🔴 2026-09-10 — 여기에 **「벽은 세로로 이어지고 지면은 가로로 이어진다」**고 적혀 있었다.
#    앵커를 재 보니 사실이 아니다 — `anchors/stage1_rift_entrance.png` 의 벽은 가로 벽돌이다.
#    그 한 줄이 [INNER] 에 `tall vertical courses` 로 내려가 **유적 석조**를 그리게 했고,
#    씬 ① 이 버리기로 한 바로 그 어휘였다. 규약이 씬 정체를 이긴 자리다.
#
#    ✅ 벽과 지면을 가르는 것은 방향이 아니라 **단위 크기 · 명도 · 상판 유무**다:
#         벽    작은 벽돌 · 평균 단 2.56 · 상판 없음(얇은 립만)
#         지면  큰 슬래브 · 평균 단 3.46 · 밟혀 닳은 상판 띠
#       (앵커 실측 2026-09-10. 벽 패널 4716px / 지면 띠 1431px)
#
# 🔴 gen3 의 미해결을 여기서 닫는다 — 그때는 벽이 [MATERIAL](배경 재질)을 그대로 썼고
#    00_WORKFLOW.md 가 "같은 문제가 남아 있다"고 적어 두었다.
#    이제 [WALL-MATERIAL] 을 먼저 보고, 없으면 GROUND-MATERIAL, 그다음 MATERIAL 로 떨어진다.
#    지면 어휘(floor slabs)를 벽에 그대로 쓸 수 없으므로 씬이 벽 재질을 따로 적는다.
#
# ⚠️ 벽은 지면보다 한 단계 더 어둡고 조용해야 한다. 화면에서 차지하는 면적이 가장 넓어서,
#    여기가 시끄러우면 다른 무엇을 조용하게 만들어도 화면이 안 정리된다.
#
# 💡 윗면 층을 지면보다 두껍게 준다 — 벽은 두께가 보여야 벽으로 읽힌다.
#
# 🔴 **지면 다음에 뽑는다** ([FOLLOWS]). 같은 재질이어도 따로 뽑으면 둘이 갈리는데,
#    먼저 만든 타일셋을 기준으로 삼으면 이어 붙게 나온다.
#    이미지 쪽에서 앵커가 하는 일을 타일셋에서는 이 줄이 한다.
#    ⚠️ 그래서 **순서가 규약이다.** 벽을 먼저 뽑으면 벽이 기준이 되고, 지면이 벽을 따라온다.
#
# 📌 벽은 [MOTIF] carved 가 가장 잘 붙는 자리다 — 벽에 손으로 새긴 이름(3권 21장).
#    다만 타일에 글자를 구우면 반복되므로, 글자는 prop_decor/prop_interactive 로 얹는다.
# ══════════════════════════════════════════════════════════════

[KIND] tileset
[PARAMS] PARAMS-TILESET
[USE-SCENE] WALL-MATERIAL, GROUND-MATERIAL, MATERIAL
[COUNT] 1

# ══════════════════════════════════════════════════════════════
# 🔴 2026-09-09 — **[FOLLOWS] 를 뺐다가 되돌렸다.** 네 라운드의 실측이다.
#
#     확정 지면(2차)  평균 단 2.87   위3단 0.00%
#
#     벽 1차 (체인)   4.23  위3단 11.69%  교집합 23.2%   계조 살아 있음
#     벽 2차 (체인)   3.66  위3단  0.00%  교집합 79.7%   구조 어휘 추가
#     벽 3차 (체인)   3.27  위3단  0.00%  교집합 76.7%   deep dark 추가   ← 채택
#     벽 4차 (체인X)  1.02  위3단  0.00%  교집합 31.0%   🔴 한 색이 97.7% — 납작해졌다
#
# 🔑 **체인은 밝기 앵커가 아니라 재질 앵커였다.** 빼니까 규약대로 어두워지긴 했는데
#    (1.02 < 2.87) **그림이 사라졌다** — 한 색이 97.7%면 벽이 아니라 검은 면이다.
#    「ΔE 0 도 합격이 아니다」와 같은 실패다(10_BIBLE/04 §5). 어두움은 얻고 재질을 잃었다.
#
# ⚠️ 그래서 **3차를 채택했고, 규약 한 줄은 미달로 남는다** —
#    이 파일 머리말의 "벽은 지면보다 한 단계 더 어둡고"가 안 지켜졌다(벽 3.27 > 지면 2.87).
#    지키려면 체인을 빼야 하고, 그러면 재질이 죽는다. **둘 다 가지는 길을 아직 못 찾았다.**
#    다음에 시도할 것: transition_size 0.4 → 0.15 (윗면 밝은 띠가 평균을 올리고 있다)
#                     또는 지면을 한 단 밝게 (지금 지면이 2.87 로 매우 어둡다)
# ══════════════════════════════════════════════════════════════

# ══════════════════════════════════════════════════════════════
# 🔴 2026-09-10 5차 — **[FOLLOWS] 를 뺐다. 이번엔 이유가 다르다.**
#
#    4차를 반영하다가 알았다 — `base_tile_id` 는 **이어 붙게 그려 주는 장치가 아니라
#    base tile 을 그대로 복사해 오는 장치다.** 실측:
#
#        지면 wang_0  ==  벽 wang_0        다른 픽셀 0 / 1024
#        벽 3차 → 4차  wang_0 변경          0 / 1024   (나머지 14칸은 33~144 씩 바뀜)
#
#    🔑 **wang_0 은 「순수 속」 타일이고, 화면의 벽은 거의 전부 그 칸이다.**
#       ArtTestStageBuilder 의 LayStrip 은 윗줄만 wang_12 이고 아래를 전부 wang_0 으로 채우며,
#       BuildVerticalWall 은 6단x2열 전부 wang_0 이다.
#       즉 **네 라운드 동안 화면에 세워진 벽은 지면 타일이었고 한 번도 안 바뀌었다.**
#       사용자가 「벽 아트가 맘에 안 든다」고 한 그림은 벽 타일셋이 아니었다.
#
# ⚠️ 위 [FOLLOWS] 주석의 해석 — *"체인은 밝기 앵커가 아니라 재질 앵커였다"* — 은 **틀렸다.**
#    체인을 뺀 4차에서 「한 색 97.7% 로 납작해졌다」고 적은 것은 재질을 잃은 것이 아니라
#    **그때 처음으로 벽 고유의 wang_0 을 본 것**이다. 그 칸을 벽이 직접 그린 유일한 라운드였다.
#    📌 지표(평균 단·교집합)는 128x128 전체를 재므로 **한 칸이 통째로 남의 것이어도 안 보인다.**
#       「교집합 지표는 구조를 못 본다」의 세 번째 사례다.
#
# 💡 그래서 이번 라운드는 **체인 하나만 움직인다**(W6). 문구(가로 벽돌 어긋쌓기)와
#    shading = flat 은 4차 그대로 둔다 — 4차 문구는 14칸에서 이미 값을 했다.
#    이전에 체인을 뺐을 때와 조건이 다르다: 그때 문구는 `tall vertical courses` 였다.
# ══════════════════════════════════════════════════════════════

# 🔴 2026-09-10 5차 결과 — **뺐다가 되돌렸다. 이번에도 대가가 왔다.**
#
#     벽 4차 (체인O)  평균 단 3.30  쓴 단 6/10  최빈 4단 35.4%  교집합 76.2%  ← 채택
#     벽 5차 (체인X)  평균 단 0.65  쓴 단 3/10  최빈 1단 59.2%  교집합 31.6%
#
#    ✅ 5차는 목적을 이뤘다 — **wang_0 이 벽 고유가 됐고 벽돌 구조도 살아 있다.**
#       이전 체인X 라운드(한 색 97.7%, 구조 없음)와 다르다. 새 문구가 값을 했다.
#    ❌ 그런데 **램프 아래 3단만 쓴다.** 화면에서는 거의 검은 면에 벽돌 선만 남는다.
#
# 🔑 **체인이 밝기를 붙들고 있었다.** 씬 [WALL-MATERIAL] 의 `deep dark` 는 2026-09-09 에
#    **체인이 있는 상태**에서 벽이 지면보다 밝길래(3.66 vs 2.87) 넣은 **보정**이다.
#    체인을 빼면 그 보정만 남아 이중으로 어두워진다. 원인을 없앴으면 보정도 빼야 한다.
#
# ⚠️ 그래서 4차(체인O)를 채택하고 되돌린다. **wang_0 이 지면 것이라는 결손은 남는다** —
#    다음 라운드는 체인을 빼고 `deep dark` → `dark` 를 같이 내리는 것이다(목표 평균 단 2.0~2.6,
#    앵커 벽이 2.56). 사용자 판단으로 2026-09-10 보류.
[FOLLOWS] tileset_ground
[OVERRIDE]
# 🔴 2026-09-07 — `2` 였다. 이 값은 비율(0.0~0.5)이라 **범위 밖이었다.**
#    지면이 0.25 이므로 벽은 그보다 두꺼운 0.4 로 둔다. 뜻은 그대로고 눈금만 맞췄다.
transition_size = 0.4

# 🔴 2026-09-10 4차 — `shading` 을 basic → flat 으로 내린다. **[PARAMS-TILESET] 이 아니라
#    여기(벽 [OVERRIDE])에 둔다** — 거기를 고치면 지면까지 따라 바뀌고, 지면은 유지 결정이다.
#
#    사용자 판정: **블록이 둥글게 부풀어 돌이 아니라 쿠션으로 읽힌다.**
#    타일 한 칸마다 basic shading 이 볼록 명암을 넣고 selective outline 이 테두리를 둘러
#    32px 칸이 그대로 「단추」가 됐다. 앵커의 벽은 정반대다 — 면이 평평하고 벽돌은
#    얕은 줄눈 한 줄로만 갈린다(단3 이 60%, 나머지는 줄눈·결로 흩어진다).
#
# ⚠️ `detail` 은 medium 그대로 둔다. low 로 같이 내리면 벽돌 켜 자체가 사라져
#    4차(체인 X)의 **한 색 97.7%** 납작함을 다시 부른다. 어두움을 얻고 재질을 잃는 실패다.
#    이번 라운드는 **shading 한 칸만** 움직인다 (W6).
shading = flat

# ══════════════════════════════════════════════════════════════
# 🔴 2026-09-09 — **벽과 지면이 안 갈렸다.** 사용자 판정(ArtTest 씬) + 실측:
#
#     램프 히스토그램 교집합  81.0%     (100% = 색 분포가 완전히 같다)
#     평균 단 차이            0.45 단
#
# 원인은 W7 이다 — **구분이 주석에만 있었다.** 이 파일 위쪽에
#     "벽은 지면보다 한 단계 더 어둡고 조용해야 한다"
# 라고 적혀 있는데 **[INNER] 문구에는 그 말이 한 글자도 없었다.**
# 집행(팔레트)도 [FOLLOWS](이어 붙임)도 「달라야 한다」는 말을 대신해 주지 않는다.
#
# 🔑 고친 방식 — **비교가 아니라 관측으로 적는다**(W2). "지면보다 어둡게"라고 쓰면
#    모델이 지면을 보고 있어야 성립하는데, 그건 [FOLLOWS] 가 있을 때만이고 불안정하다.
#    대신 **벽에서만 관측되는 것**을 적는다: 세로로 쌓인 켜, 깊이 팬 줄눈, 방에서 가장 어두운 면.
#    🔴 2026-09-10 정정 — 방식은 옳았고 **관측이 틀렸다.** 앵커의 벽은 세로가 아니라 가로다.
#       W2(관측을 적는다)를 지키려면 관측을 **앵커에서** 떠야 하는데 머리말에서 떴다.
#    지면은 가로 슬래브·밟혀 닳은 상판이라 관측 자체가 겹치지 않는다.
#
# ⚠️ **[FOLLOWS] 는 그대로 둔다**(W6 — 한 라운드에 한 곳만). 그것을 같이 빼면
#    이번 라운드가 나아져도 **무엇이 고쳤는지 모른다.** 문구만으로 안 갈리면 그때 뺀다.
# ══════════════════════════════════════════════════════════════

[INNER]
# 🔴 2026-09-09 2차 — `the darkest surface in the room` 을 뺐다. **안 먹었다.**
#    「방」이 무엇인지 모델은 모른다. 명도는 씬의 [WALL-MATERIAL] 로 옮겼다 —
#    거기 붙이면 벽이 어디에 쓰이든 따라간다. 여기는 **구조**만 말한다.
# 🔴 2026-09-10 4차 — **`tall vertical courses` 를 버렸다. 앵커에 없는 구조였다.**
#
#    사용자 판정: 벽/지면은 갈리지만 **벽 아트가 컨셉과 안 맞는다.** 앵커를 다시 재서 확인했다 —
#    `anchors/stage1_rift_entrance.png` 의 벽은 **가로로 눕힌 작은 벽돌이 어긋나게 쌓인 켜**이고
#    줄눈이 아주 얕다. 세로 켜는 한 장도 없다.
#
#    🔑 **세로 켜는 유적 어휘였다.** 씬 ① 이 「폐허·유적을 버린다」로 정체를 잡아 놓았는데
#       (the underside of an ordinary town) 이 블록만 신전 석조를 말하고 있었다.
#       벽/지면을 가로·세로로 가르려던 것이 **씬 정체를 이긴 자리**다.
#
#    ⚠️ 그러면 벽도 지면도 가로가 되는데, 앵커는 그 둘을 다른 것으로 가른다:
#         단위 크기   벽=작은 벽돌 · 지면=큰 슬래브
#         명도        벽 2.56 단 < 지면 띠 3.46 단 (앵커 실측, 0.90 단 차)
#         상판        지면만 닳은 상판 띠가 있다
#       **가로/세로가 아니라 크기로 가른다.** 실측이 그렇게 되어 있다.
#
# 🔴 같이 뺀 것 둘 — 예산을 구조에 몰아준다:
#      `one even repeating material`  지면 [INNER] 에서 복사돼 온 말이다. 지면에선 맞지만
#                                     (밟는 면은 균질해야 한다) 벽에서는 **켜를 지운다.**
#                                     「고르게 반복되는 하나의 재질」이 곧 균질 격자다.
#      `plain and quiet`              씬 [WALL-MATERIAL] 의 `quiet and even` 과 같은 말이다.
#                                     24 단어 중 절반이 「조용함」 하나에 쓰이고 있었다.
#
# 💡 명도는 여기서 말하지 않는다 — 2차의 교훈 그대로 씬 [WALL-MATERIAL] 이 갖는다.
#    여기는 **구조**만 말한다.
a flat wall of small horizontal bricks laid in staggered rows, thin shallow joints

[TRANSITION]
the broken top edge where the wall ends, its thickness showing as a narrow lip

[OUTER]
dark empty air
