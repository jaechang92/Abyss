# ══════════════════════════════════════════════════════════════
# 중경 레이어  🔴 **씬 앵커**
#
# 🔴 씬마다 이 파트를 **가장 먼저, 여러 장** 뽑아 사람이 한 장을 고른다.
#    고른 것이 그 씬의 style_image 가 되어 나머지 전부의 화풍을 결정한다.
#    앵커 한 장 다시 뽑는 값 < 그 씬 20장 다시 뽑는 값.
#
# 판정은 10_BIBLE/03-light.md §5 체크리스트로 한다. 특히:
#   - 화면 안에 광원이 보이는가            → 보이면 탈락 (L1)
#   - 방향이 있는 그림자가 있는가          → 있으면 탈락 (L2)
#   - 가장 어두운 값이 순검정인가          → 그러면 탈락 (L3/P4)
#   - 공중에 떠 있는 입자가 있는가         → 있으면 탈락 (L6)
#   - 폐허·유적으로 보이는가               → 그러면 탈락 (W2)
#   - 인물·생물이 있는가                   → 있으면 탈락
#   - 팔레트 안에 머무는가
#   - 가로 양끝이 이어지는가
#   - 캐릭터가 그 앞에 섰을 때 실루엣이 읽히겠는가
# ══════════════════════════════════════════════════════════════

[KIND] bg
[PARAMS] PARAMS-BG
[USE-SCENE] SETTING, MATERIAL, LIGHT
[USE-MOTIF] yes
[USE-TONE] yes
[WORD-BUDGET] 50
[COUNT] 1

[BODY]
# 🔴 2026-09-08 (1차) — `open at both sides` 를 더했다.
#    첫 앵커 호출의 후보 4장이 **전부 아치로 닫힌 실내 한 칸**으로 나왔다.
#    양옆이 막히면 가로로 안 이어지고, 이 층은 가로로 스크롤되는 패럴랙스다.
#    위 판정 목록에 「가로 양끝이 이어지는가」가 있었지만 **판정에만 있고
#    프롬프트에는 없었다.** 광원과 달리 이건 긍정으로 부탁할 수 있는 항목이다 —
#    판정으로만 막으면 될 때까지 다시 뽑는 값을 낸다.
#
# 🔴 2026-09-08 (2차) — 그 말을 **버렸다.** 2라운드는 멀쩡했는데 3라운드에서 터졌다.
#
#    3라운드에서 시점 문구를 고쳐 4장 전부 정측면이 되자, 같은 `open at both sides` 가
#    **4장 전부에 구멍을 뚫었다** — 화면 중간 높이(y 34~67)에 평평한 밝은 블록이
#    좌우 끝까지 이어진 채로. 네 장이 같은 대역이었으니 내용이 아니라 문구에 대한 반응이다.
#
#    🔑 **같은 문구가 시점이 바뀌자 다른 뜻이 됐다.** 3/4 부감에는 물러나는 면이 있어서
#       「열려 있다」가 구도로 표현된다. 평평한 입면에는 물러날 면이 없으므로
#       열려 있음을 그리려면 **구멍을 뚫는 수밖에 없다.**
#    📌 결함은 2라운드에도 이미 있었다. 조건을 못 만나서 안 보였을 뿐이다 —
#       그래서 판정 체크리스트로는 잡을 수 없는 종류였다. 통과한 라운드가
#       그 문구가 안전하다는 증거가 아니다.
#
#    → `continuing past both edges`. 뜻(화면 밖으로 이어진다)은 같고
#      「열림/구멍」이라는 명사가 없다. 4단어로 길이도 같아 예산이 그대로다.
#
# ⚠️ 캔버스를 넓혀 해결하지 않는다. build.py candidates_for() 가 최장변 170 을
#    넘으면 한 호출당 후보를 4장 → **1장**으로 준다. 비용은 호출당이라 그대로다.
#    비율로 고치면 앵커 한 장에 25 gen 을 내게 되고, 고르는 작업에 고를 것이 없어진다.
the mid layer behind the play area, continuing past both edges
