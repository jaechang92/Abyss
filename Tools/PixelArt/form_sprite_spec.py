# -*- coding: utf-8 -*-
"""
form_sprite_spec.py — 폼(플레이어) 스프라이트 규격 SoT.

🔴 이 파일이 규격의 단일 출처다. 생성기(generate_form_sprites.py)와 문서
(Docs/technical/sprite-spec.md)가 여기서 값을 읽거나 인용한다.
값을 고치면 반드시 생성기를 다시 돌릴 것.

── 규격이 왜 이 숫자인가 ─────────────────────────────────────────
PPU 16          기존 적 스프라이트 전부가 spritePixelsToUnits=16이다. 섞으면 같은 화면에서
                픽셀 크기가 달라 보인다(도트 아트가 깨졌다고 느끼는 가장 흔한 원인).
16 x 32 px      플레이어 콜라이더가 1 x 2 유닛(PrefabBuilder.Player)이라 PPU 16에서 정확히
                16 x 32 px다. 스프라이트를 콜라이더보다 크게 그리면 안 맞는 판정이 되고,
                작게 그리면 허공에 뜬다. **콜라이더가 규격을 정한다.**
피벗 (0.5, 0)   발밑 중앙. 지면에 서는 캐릭터는 발이 기준이어야 층·경사에서 안 뜬다.
외곽선 1px      적 스프라이트가 쓰는 #1A1A1A 그대로. 배경이 어두운 게임이라 외곽선이 없으면
                실루엣이 배경에 먹힌다.
────────────────────────────────────────────────────────────────
"""

PPU = 16
WIDTH = 16
HEIGHT = 32
PIVOT = (0.5, 0.0)

# 외곽선 — 적 스프라이트와 공유(generate_enemy_sprites.py OUTLINE과 같은 값)
OUTLINE = (26, 26, 26, 255)      # #1A1A1A
TRANSPARENT = (0, 0, 0, 0)

# 공통 살색 — 네 폼이 같은 사람이라는 것을 색으로 붙든다.
# 🔑 폼은 "다른 캐릭터"가 아니라 **같은 사람이 형태를 바꾼 것**이다(00-concept USP-2).
#    얼굴·손의 살색을 공유하면 그 전제가 실루엣만 바뀌어도 유지된다.
SKIN = (214, 176, 140, 255)
SKIN_SHADE = (168, 132, 100, 255)

# ── 폼별 팔레트 ──────────────────────────────────────────────
# 시너지 축 색(SynergyAxis)에서 가져온다 — 플레이어가 HUD에서 이미 배운 색이다.
#   불꽃 #FF7630 · 심연 #A36BF5 · 수호 #FFCC52
# 각 폼 3단계(밝음/기본/그림자)로만 쓴다. 도트에서 단계를 늘리면 지저분해진다.
PALETTES = {
    "dark_blade": {      # 암흑 검사 — 불꽃 축. 검붉은 갑주 + 달아오른 칼날
        "light": (198, 86, 54, 255),
        "base":  (140, 52, 38, 255),
        "shade": (84, 30, 24, 255),
        "accent": (255, 118, 48, 255),   # 칼날 열
    },
    "void_archer": {     # 공허 궁수 — 심연 축. 자주빛 경장 + 공허 시위
        "light": (163, 107, 245, 255),
        "base":  (108, 66, 172, 255),
        "shade": (62, 38, 104, 255),
        "accent": (206, 168, 255, 255),  # 시위 광
    },
    "ancient_shield": {  # 고대 방패병 — 수호 축. 놋쇠 갑주 + 큰 방패
        "light": (255, 204, 82, 255),
        "base":  (176, 134, 48, 255),
        "shade": (104, 78, 30, 255),
        "accent": (240, 232, 208, 255),  # 방패 문양
    },
    "void_thrower": {    # 심연 투척사 — 심연 축이지만 궁수와 구분되어야 한다.
        "light": (122, 200, 190, 255),   # 청록으로 갈라 놓는다
        "base":  (68, 138, 132, 255),
        "shade": (36, 84, 82, 255),
        "accent": (168, 255, 240, 255),  # 투창 광
    },
}

# ── 실루엣 규약 ──────────────────────────────────────────────
# 🔑 색이 아니라 **실루엣**으로 구분되어야 한다. 색약 대응이기도 하고(00-concept §디자인),
#    전투 중에는 틴트·피격 플래시로 색이 순간적으로 뭉개지기 때문이다.
SILHOUETTE_RULES = {
    "dark_blade":     "어깨가 넓고 한쪽에 긴 검. 세로로 길게 뻗는 선 하나",
    "void_archer":    "가장 마르다. 활이 몸 옆에 곡선으로 — 유일한 곡선 실루엣",
    "ancient_shield": "가장 넓다. 방패가 몸 절반을 덮어 사각형 덩어리",
    "void_thrower":   "망토가 아래로 퍼진다. 삼각형(위가 좁고 아래가 넓다)",
}
